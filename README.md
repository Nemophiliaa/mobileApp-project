## Jalankan Project

1. **Backend (`api`)**  
```bash
cd api
npm install      # install dependencies
node server.js   # jalankan backend


cd ../myJsonApp
npm install      # install dependencies
ionic serve      # jalankan frontend
